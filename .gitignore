node_modules/
dist/
dist_bak*/
dist_verify*/
build/
*.log
.env
.env.local
backend/data/
.DS_Store
*.tsbuildinfo

# Local-only design conversation transcript — never pushed to GitHub.
chats/

# Added by the Vercel CLI. Its `.env*` also matched the tracked
# frontend/.env.example, which documents the required variables and is
# meant to be in the repo — hence the negation.
.vercel
.env*
!.env.example
